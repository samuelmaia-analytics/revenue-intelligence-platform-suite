# Release v1.0.0
Date: 2026-03-05

## Highlights
- Flagship monorepo positioning finalized (`modules/`, `apps/`, `platform/`, `packages/common`, `reports/showcase`).
- Executive dashboard upgraded to telemetry-backed KPIs.
- Action adoption observability added with CSV and JSONL logs.
- Shared contracts standardized in `packages/common/contracts`.
- CI upgraded to root checks + per-module quality matrix + executive smoke tests.

## Major Changes
1. Added enterprise-like telemetry connector:
   - `platform/connectors/sqlite.py` (architectural wrapper)
   - `platform_connectors/sqlite.py` (runtime connector)
2. Added observability logger for executive actions:
   - `platform/observability/action_adoption.py` (wrapper)
   - `platform_observability/action_adoption.py`
3. Added shared contracts:
   - `packages/common/contracts/showcase_summary.schema.json`
   - `packages/common/contracts/action_adoption_event.schema.json`

## Breaking/Behavioral Notes
- Executive KPI board now uses telemetry-backed NRR/churn from SQLite mock instead of in-page proxy calculations.
- `scripts/run_showcase_demo.py` now generates `reports/showcase/enterprise_telemetry.sqlite`.

## Realized Business Delta (Published)
- Decisioning quality delta: leadership prioritization is now telemetry-backed instead of static/mock-only KPI interpretation.
- Quantified exposure baseline:
  - `total_value_at_risk_usd`: `144490.04`
  - `top_50_value_at_risk_usd`: `29130.33`
  - `nrr_latest`: `1.20`
- Source of truth: `reports/showcase/summary.json`

## Validation
- Root tests + contracts: pass locally.
- Showcase generation script: pass locally.
